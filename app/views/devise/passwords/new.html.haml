%h1
  Forgot your password?
%p.description
  You can retrive your pastword from here

- form_for resource_name, resource, :url => password_path(resource_name) do |f|
  = f.error_messages
  .field
    = f.label :email
    = f.text_field :email
  .buttons
    = f.submit "Send me reset password instructions", :class => "save"


= render :partial => "shared/devise_links"
