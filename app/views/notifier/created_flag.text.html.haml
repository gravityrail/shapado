-link = url_for(:host => @group.domain, :controller => "admin", :action => "moderate")

= t("notifier.global.hi", :user => @user.login)
%br
= t("notifier.created_flag.message1", :group => @group.name)
%br
= t("notifier.created_flag.message2")
%p
  = @reason

= link_to(t("notifier.created_flag.message4"), link)
