=yield

= "_"*80
- domain = @group ? @group.domain : AppConfig.domain
= t('.message', :domain => domain)
= url_for(:host => domain, :controller => "users", :action => "edit", :id => @user, :anchor => "notifications")
