

= render "flags/#{@resource.class.to_s.tableize.singularize}", {:resource => @resource}

= render "flags/form", :flag => @flag, :source => @source, :flaggeable => @resource, :form_id => "question_flag_form"
