- if logged_in? && (current_user.facebook_login? || current_user.twitter_login? || current_user.identica_login?)
  = render :partial => "shared/suggestions", :locals => {:limit => 5}
  .more
    = link_to t("scaffold.more"), suggestions_path
    .clear
