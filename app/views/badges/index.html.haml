%h1 Badges
%p.description= t(".description")
.content
  %ul
    - @badges.each do |badge|
      %li
        .badge_item{:class => badge.type}
          = pjax_link_to badge.name, "badges", {'controller' => 'badges', 'action'=>'show','id'=>badge.to_param}, {:class => "badge", :title => badge.description}

          .counter
            == × #{badge["count"]}
        .badge_description
          =badge.description
        .clear
